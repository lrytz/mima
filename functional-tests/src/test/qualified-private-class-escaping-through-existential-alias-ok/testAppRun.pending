Lib.make hands the list out as Any, so the bound in Lib.K is the only trace
of foo.C. The client casts to it and calls bar through the bound. mima
misses the bound, so it names nothing and the app dies on C.bar(Int).
