# Scala 2 drops the forwarder along with the method, and mima reports it missing,
# as it does in object-public-method-becomes-package-private-nok.
static method make(Int)Int in class foo.Lib does not have a correspondent in new version
private[..] method make(Int)Int in class foo.Base is not part of the API in new version, so a later change to it will no longer be reported, though it would break clients calling it today
