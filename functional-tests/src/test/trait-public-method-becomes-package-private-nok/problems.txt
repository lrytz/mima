private[..] method bar(java.lang.Object)java.lang.Object in trait bar.A is not part of the API in new version, so a later change to it will no longer be reported, though it would break clients calling it today
