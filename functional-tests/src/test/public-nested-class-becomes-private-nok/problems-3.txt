# Scala 3 drops the outer pointer of a private inner class that doesn't use it, so the
# constructor loses its parameter; Scala 2 keeps it.
method use()foo.A#B in class foo.A does not have a correspondent in new version
private class foo.A#B is not part of the API in new version, so a later change to it will no longer be reported, though it would break clients using it today
method this(foo.A)Unit in class foo.A#B does not have a correspondent in new version
