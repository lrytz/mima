method bar(Int)Int in class foo.Foo does not have a correspondent in new version
private[..] class foo.Foo is not part of the API in new version, so a later change to it will no longer be reported, though it would break clients using it today
